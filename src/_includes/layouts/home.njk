{% extends 'layouts/base.njk' %}

{% block content %}
  <div class="container" role="banner">
    <h1>{{ title }}</h1> 
  </div>

  <section role="main" class="container feature hl article">
		{{ content | safe }}
		{% include "partials/components/post-latest.njk" %}
  </section>

{% endblock %}
